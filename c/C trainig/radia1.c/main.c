#include <stdio.h>
#include <stdlib.h>

int main()
{
    const float pi = 3.14159;
    float radie, area;

    /* läs in första värde till radie*/

    printf("ge radie (restart with 0)");
    scanf("%f, &radie");

    /* beräkna area och läs nytt värde*/

    while (radia != 0)

        /*

    printf("\n");
    return 0;
}
